# OAuth

A project testing setting up OAuth.

# Running the App

Starting the backend:

```bash
npm install
npm run create-db
npm run build
```

Starting the frontend:

```bash
npm run install-frontend
npm run dev
```